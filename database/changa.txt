═══════════════════════════════════════════════════════════════════════════════
 CHANGA  —  what YOU have to change, by hand, outside the code
═══════════════════════════════════════════════════════════════════════════════

 Updated 22 September 2026 with the order-chain and new-customer work
 (section C, first). Section B is 21 September, A is 20 September and 1-4 are
 17 September. All of them still stand.


───────────────────────────────────────────────────────────────────────────────
 C.  22 SEPTEMBER — ONE KEY, TWO MIGRATIONS, AND A WORD TO YOUR ORDER DESK
───────────────────────────────────────────────────────────────────────────────

 C1  GET A READER KEY, OR THE NEW CUSTOMER SCREEN ONLY TAKES PICTURES

     Photographing a CNIC and a business card and having the form fill itself
     needs something that can read a photograph. Nothing on this system can
     today: "Gemini:ApiKey" in appsettings.json is an empty string.

     It is free and it takes two minutes:

        1. open  https://aistudio.google.com/apikey  and sign in with a Google
           account (vizo.com.pk@gmail.com is the obvious one)
        2. press "Create API key" and copy it
        3. put it in backend/vizo-backend/appsettings.json:

               "Gemini": { "ApiKey": "AIza….", "Model": "gemini-2.0-flash", … }

           or, on the API host, as the environment variable

               Gemini__ApiKey = AIza….

        4. restart the API

     The free tier is generous -- a few hundred documents a day -- and reading
     one customer is two pictures. Nothing else in the app needs the key; the
     AI reports have been waiting for it since September and will start
     working at the same time.

     UNTIL IT IS SET, the new-customer screen says so on its first page, still
     takes and files the photographs, still makes the one PDF, and the
     salesperson types the details. Nothing is broken; it is just manual.

     WHAT GOES TO GOOGLE when it is on: the CNIC and business-card photographs
     of the shop you are opening, and nothing else. No prices, no customers,
     no ledger. Google's API terms say paid-tier data is not used for
     training; the free tier is used for improving their models. If that
     matters to you, use a paid key -- it is the same key field.

 C2  BOTH MIGRATIONS ARE ALREADY RUN — on Neon only

     backend/database/21_order_chain_and_receiving.sql
     backend/database/22_customer_documents.sql

     RUN AND VERIFIED on the live database on 22 September, because you said
     to run them. ANY OTHER ENVIRONMENT needs them before the new API goes up
     there. Both are safe to run twice.

     What 21 did that people will SEE:
       · the order chain is seven steps, not ten. "Seen by Warehouse", "On way
         to Order Dept" and "Packaging" are gone, and "Received at Order Dept"
         now reads "Processing in Order Dept"
       · SEVEN LIVE ORDERS that were standing in the removed steps are now at
         "Processing in Order Dept", each with a line in its own history
         saying why
       · money coming in is Cash, Credit, Meezan or Faisal -- everywhere money
         is received. Nothing was deleted; the old methods still exist for
         money going out
       · ORD-26-0171 and INV-26-8893 are deleted, as you asked

     What 22 did: eight columns on "Party" for a customer's six document
     pictures and the PDF they are bound into. Empty for every existing
     customer.

 C3  TELL THE ORDER DESK THAT DISPATCH NOW TAKES THE STOCK OFF

     This is the biggest change in the round and the one that will surprise
     somebody. Pressing Dispatched asks WHICH PLACE the order is going out of
     and takes the goods off that shelf there and then. If the shelf is short
     it refuses and names what is missing.

     Two things follow from that:

       · THE PACKING SCREEN IS GONE. It was the only place stock used to leave
         from, it ran on a status that no longer exists, and two screens
         taking the same goods off the same shelf is how a count goes wrong.
       · THE DISPATCH SCREEN NOW COMES AFTER the step instead of before it --
         it is where the courier, the bilty and the COD are recorded once the
         goods are on a van.

     WHO CAN DISPATCH: the order department, the accountant and you.

 C4  THE PAST ORDERS ARE STILL WRONG — one script, your word

     26 orders were dispatched or delivered before today without ever moving
     stock, so every product on them is still counted as being on a shelf. The
     correction is one script; it changes real shelf counts, so it waits for
     you. Say the word and I will show you the list before running anything.

 C5  "RECORD PAYMENT" ON AN INVOICE NEVER RECORDED ANYTHING

     Not a change for you to make -- a thing to know. That button produced a
     form, said "Payment recorded" and wrote nothing at all, to any table, for
     anybody who ever pressed it. If your accountant has been using it,
     nothing they entered exists.

     It is off the sales panel now, as you asked, and for the accountant and
     you it opens the RECEIPT VOUCHER with the customer already chosen --
     which is the screen that really posts money against their invoices.

     NO ENVIRONMENT VARIABLES CHANGED in this round, apart from the reader key
     in C1.


═══════════════════════════════════════════════════════════════════════════════
 21 SEPTEMBER — still current
═══════════════════════════════════════════════════════════════════════════════

───────────────────────────────────────────────────────────────────────────────
 B.  21 SEPTEMBER — FOUR THINGS, AND THE FIRST ONE IS URGENT
───────────────────────────────────────────────────────────────────────────────

 B1  ASSIGN YOUR CUSTOMERS TO THE RIGHT SALESMAN — TODAY

     You asked that a salesman only see his own customers in every dropdown,
     the way his Customers screen already works. That is done. On today's data
     it means:

         Sara Khan               7 customers
         Zara Malik              1
         Imran Iqbal             0      <-- cannot raise an order at all
         Muhammad AMMAR KAMRAN   0      <-- cannot raise an order at all

     Because the customer box is the first thing on the order form, a rep with
     an empty list is stuck. Two of your four are in that state.

     WHY: of the 18 shop accounts, 7 are assigned to Sara, 5 are assigned to
     BILAL AHMED (order department), 4 to HASSAN RAZA (accountant) and 2 to
     nobody. Nine of them are owned by people who never take an order.

     THE FIX, five minutes, no deploy, no sign-out:
         People → Customers → open each account → set the SALESMAN to the rep
         who actually calls on that shop → Save.

     I did not reassign them myself: who calls on which shop is your business,
     not something the database can be asked.

 B2  THE MIGRATION IS ALREADY RUN — on Neon only

     backend/database/20_places_rights_and_returns.sql

     RUN AND VERIFIED on the live database on 21 September, because you said to
     run it. ANY OTHER ENVIRONMENT — a colleague's copy, a staging database —
     needs it before the new API goes up there. Safe to run twice.

     What it did:
       · deleted the "In Transit" location AND the location type, after moving
         ORD-26-0158 and INV-26-8878 to Karachi Order Department and removing
         240 duplicate units of stock (see convey.txt R1 — those 240 were
         counted twice, once here and once in Shop 2 where they really are)
       · took invoices.create off the SALES role (they keep invoices.view, so a
         rep can still print his own bill)
       · took returns.sales off SALES and off the ORDER DEPARTMENT, leaving it
         with you and the accountant
       · made "SalesReturn"."InvoiceId" nullable, so a return can cover several
         orders
       · renamed the INVOICED status to "Invoiced/Edit" (the internal key is
         untouched, so nothing else had to change)

     DEPLOY ORDER: run it BEFORE or WITH the new API build, never long after.
     The old build copes with all of it.

 B3  EVERYBODY SIGNS OUT AND BACK IN — the same day

     Same reason as migration 18 in September: permissions travel inside the
     JWT and a token lasts eight hours. Until a salesman signs out and back in,
     his old token still carries invoices.create and returns.sales, and the
     API will let him bill an order with it. Signing out fixes it at once;
     doing nothing fixes it by tomorrow morning.

     NO ENVIRONMENT VARIABLES CHANGED in this round. Nothing to set on the host.

 B4  TELL YOUR ACCOUNTANT THAT BILLING IS THEIRS NOW

     Nobody in sales can press Invoiced any more. If Hassan or Nadia do not
     know that they are the ones who bill a confirmed order, orders will sit at
     Confirmed and nobody will be waiting on anything.

     They get a notification on every confirmation ("Waiting for accounts to
     invoice it"), and the step is called "Invoiced/Edit" because at that
     moment they may also correct the order — lines, quantities, prices —
     before the bill is cut. The invoice rebuilds itself to match.


═══════════════════════════════════════════════════════════════════════════════
 20 SEPTEMBER — still current
═══════════════════════════════════════════════════════════════════════════════

───────────────────────────────────────────────────────────────────────────────
 A.  PRODUCT WORK, 20 SEPTEMBER — three things
───────────────────────────────────────────────────────────────────────────────

 A1  DROP THE OPENING COST COLUMN — after the new API is deployed

     You asked for the column to be dropped. Everything that USED it is gone
     already; the column itself is left for you because of deploy order.

     The API running today selects "OpeningCost" on every product query. If
     the column disappears while that build is still live, every product
     screen answers 500 until the new build replaces it. The new build never
     reads it. So:

        1. deploy the new API (the push of 20 September)
        2. then run section 2 of
               backend/database/19_product_pricing.sql
           which is one line:
               ALTER TABLE "Product" DROP COLUMN IF EXISTS "OpeningCost";

     Section 1 of that file — the Duty and Margin columns, and every existing
     product's margin filled in — is ALREADY RUN on Neon. Any other database
     (a local copy, staging) needs the whole file.

     Dropping a column cannot be undone. The values in it are all copies of
     the cost price in any case, but take a backup first if in doubt.

 A2  npm install IN THE FRONTEND

     Two new packages, for the barcode camera on phones without a built-in
     reader (Safari, iPhone, Firefox):

         @zxing/browser   @zxing/library

     They are in package.json; `npm install` in vizo-erp picks them up. The
     build fails on the import without them. Vercel installs on its own.

 A3  THE CAMERA ONLY WORKS ON HTTPS

     Browsers refuse the camera on a plain http page. The live site is https
     (Vercel), so nothing to do there — but on a phone pointed at a laptop on
     the office network (http://192.168…) "Scan barcode" will say the camera is
     blocked. That is the browser, not the app. Typing a barcode still works
     everywhere.

     NO ENVIRONMENT VARIABLES CHANGED in this round.


═══════════════════════════════════════════════════════════════════════════════
 17 SEPTEMBER — still current
═══════════════════════════════════════════════════════════════════════════════

 Nothing in this file is optional if you want that work to run. Everything in
 it is a setting, a migration or a click — there is no code left to write.

 Read section 1 before you deploy anything. Four things; the first is already
 done on Neon and the other three are yours. Ten minutes in all.


───────────────────────────────────────────────────────────────────────────────
 1.  DO THESE FOUR, IN THIS ORDER
───────────────────────────────────────────────────────────────────────────────

 1.1  THE MIGRATION IS ALREADY RUN — on Neon only

      backend/database/18_sales_scope_returns_and_places.sql

      RUN AND VERIFIED on the live Neon database on 17 September, because you
      said to run it in full. ANY OTHER ENVIRONMENT — a colleague's local copy,
      a staging database — still needs it, or the API will 500 on every invoice
      and party screen. It is safe to run twice.

      What it did that people will SEE the next morning:

        · it renames LOC-01 "Warehouse" to "Karachi Warehouse" and LOC-02
          "Order Department" to "Karachi Order Department"
        · it adds "Lahore Warehouse" (LOC-06) and "Lahore Order Department"
          (LOC-07)

      Rename them, deactivate them or add Faisalabad beside them from
      Administration → Locations. Nothing in the code knows any location's id
      or name.

      DEPLOY ORDER STILL MATTERS ELSEWHERE. The migration adds two columns the
      new code selects on every invoice and every party query —
      "SalesInvoice"."PdfDeliverable" and "Party"."CreatedByUserId". Wherever
      it has not been run yet, run it BEFORE the new API goes up.

      It also did three things you cannot see but will feel:

        · grants invoices.view, invoices.create and returns.sales to the Sales
          role. THIS IS THE FIX for "Could not load invoices and refund
          methods" and for a salesman who could not print his own bill.
        · records who opened each customer account (back-filled from the
          assigned rep, which is the closest true answer that exists).
        · fills in the primary location of any warehouse or order-desk account
          that already has exactly one.


 1.2  EVERYBODY SIGNS OUT AND BACK IN — the same day

      Permissions travel INSIDE the JWT, as "perm" claims, and a token lasts
      eight hours (Jwt:ExpiryMinutes = 480). A salesperson holding a token
      issued before the migration still carries the old five permissions, and
      will go on being refused the invoice screen until it expires.

      Signing out and back in fixes it immediately. Doing nothing fixes it by
      tomorrow morning. There is no server-side way to force it short of
      rotating Jwt:Key, which would sign everybody out including you.


 1.3  SET THE WEBSITE ADDRESS ON THE API HOST

      New setting, because every notification now carries a real link instead
      of a bare path like "/sales/orders/42":

          App__WebBaseUrl = https://advpos-frontend.vercel.app

      Set it as an ENVIRONMENT VARIABLE wherever the API runs (Railway →
      Variables). The double underscore is ASP.NET's own convention for
      nesting; it overrides the value committed in appsettings.json, which is
      already set to the same thing as a fallback.

      Change this one line and every notification, every push and every link in
      the bell follows it. That was the point of asking for it.

      WHY IT MATTERS: a Web Push payload is opened by the service worker with
      clients.openWindow(). A bare path there resolves against the worker's own
      scope rather than against your site — it happens to work while the app is
      the only thing on that origin and stops working the day it is not. And
      when somebody forwards a notification to a colleague, a path is not a
      link at all.

      In-app it still behaves as it always did: the bell turns a link back into
      a path when it points at the site you are already on, so clicking one
      does not reload the whole application.


 1.4  ADD THE VERCEL ORIGIN TO CORS — probably already done, check anyway

      appsettings.json now lists it:

          "Cors": { "AllowedOrigins": [ ..., "https://advpos-frontend.vercel.app" ] }

      It was NOT there before: the list was localhost:3000, localhost:3000 over
      https, and www.vizo.com.pk. If the live frontend has been working, then
      something on the host is already overriding this — check for a
      Cors__AllowedOrigins__3 variable and make sure the two do not disagree.

      A missing origin fails the browser's pre-flight before any of our code
      runs, which looks exactly like a broken API and is not one.


───────────────────────────────────────────────────────────────────────────────
 2.  THINGS TO CHECK ONCE, AFTER DEPLOYING
───────────────────────────────────────────────────────────────────────────────

 2.1  THE SIX STRANDED ORDERS ARE INVOICED — check the numbers suit you

      Done on your say-so, through the real endpoint rather than by hand, so
      the numbering, the lines, the PDF, the activity log and the notifications
      all went the normal way:

          ORD-26-0140  ->  INV-26-8888   dated 10 Aug
          ORD-26-0141  ->  INV-26-8889   dated 13 Aug
          ORD-26-0143  ->  INV-26-8890   dated 14 Aug
          ORD-26-0168  ->  INV-26-8891   dated 15 Sep
          ORD-26-0170  ->  INV-26-8892   dated 16 Sep
          ORD-26-0171  ->  INV-26-8893   dated 16 Sep

      THE DATES ARE BETTER THAN I WARNED YOU. I said they would carry today's
      date; they do not. An invoice raised against an order is dated from the
      ORDER, so these read as the days the orders were actually taken. Only the
      numbers came off today's series. All six PDFs are on Cloudinary.

      THE SERIES MOVED from INV-26-8888 to INV-26-8894. If your accountant
      expects invoice numbers to run in date order, these six do not — they
      are the highest numbers in the book and among the oldest dates. That is
      unavoidable once a number is issued late, and it is the only thing about
      this worth a second look.

      Raising them briefly rewound five order statuses. They are back, each
      with an ORDER_STATUS_RESTORED row in the activity log, and the line that
      caused it is fixed. Full account in convey.txt §7A.

      The list should stay empty from now on. The query to check is at the foot
      of migration 18, and the "Raise invoice" button now appears on any order
      that has no invoice rather than only on a confirmed one.

 2.2  TEN INVOICES HAVE NO STORED PDF

      Of 38 sale invoices, 10 have no PdfUrl at all. Nothing is broken — the
      API renders a bill from the database whenever one is asked for, so Print
      works on all of them. They simply have no archived copy.

      Opening each one archives it. Or press "Rebuild" on the invoice screen.
      No hurry.

 2.3  PDF DELIVERY IS ON, AND THE APP NOW NOTICES

      Cloudinary blocks PDF delivery by default on accounts created since 2023.
      convey.txt has said for weeks that both of this project's accounts were
      in that state. CHECKED TODAY, and it is no longer true:

          HEAD .../advpos/documents/invoices/INV-26-8887_u7qavn.pdf → 200 OK

      Somebody has ticked the box. Good — that is the better link to hand out.

      The app used to ignore the question entirely and always open the
      Cloudinary URL, which is why Print showed an error page during the months
      the answer was no. It now stores the answer per invoice and uses it. The
      first time each old bill is opened it spends one HEAD request finding out
      and writes it down; after that it goes straight to Cloudinary.

      If you ever untick it, nothing breaks — the API serves the bytes itself.

      Six invoices are already flagged deliverable: the six raised above, plus
      one re-checked while testing. The other 32 flip the first time somebody
      opens them.


───────────────────────────────────────────────────────────────────────────────
 3.  SETTINGS SUMMARY — every knob this work added or touched
───────────────────────────────────────────────────────────────────────────────

  SETTING                        WHERE                    VALUE
  ─────────────────────────────  ───────────────────────  ──────────────────────
  App:WebBaseUrl                 appsettings.json         https://advpos-frontend.vercel.app
  App__WebBaseUrl                API host env var         same — overrides above
  Cors:AllowedOrigins[3]         appsettings.json         https://advpos-frontend.vercel.app

  Nothing was removed. Nothing else in appsettings.json changed. No frontend
  environment variable changed — vizo-erp/.env.local is untouched and still
  needs only NEXT_PUBLIC_API_BASE_URL and the VAPID public key.


───────────────────────────────────────────────────────────────────────────────
 4.  STILL UNFIXED, FROM AN OLDER CONVEY — one line, your call
───────────────────────────────────────────────────────────────────────────────

  vizo-erp/.env.example carries a VAPID public key that does NOT match the
  server's. .env.local is correct, so push works today; the danger is that
  anybody setting up a new machine by copying the template gets a browser that
  subscribes happily and a server that then rejects every send — silently.

  The fix is to copy VapidSettings:PublicKey out of appsettings.json into
  .env.example. Still not done, because a key is not mine to change and
  changing the wrong one of the two breaks the setup that works.

═══════════════════════════════════════════════════════════════════════════════
